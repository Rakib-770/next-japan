# k8s/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: next-japan-svc
  labels:
    app: next-japan
spec:
  type: NodePort
  selector:
    app: next-japan
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080 

